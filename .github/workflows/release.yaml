name: dx_data_collector - Release

on:
  push:
    tags:
      - "v*.*.*"

jobs: 
  release:
    uses: ./.github/workflows/rios_release.yaml
    with:
      working-directory: dx_data_collector
    secrets:
      ssh-key: ${{ secrets.CI_SSH_KEY }}
      bot-token: ${{ secrets.RIOS_BOT_TOKEN }}
