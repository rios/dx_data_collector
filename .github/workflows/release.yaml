name: dx_data_collector - Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  release:
    # NOTE: This workflow previously used RIOS-internal reusable workflows.
    # Replace with your preferred release workflow.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Release
        run: echo "Configure release step for your environment."
