name: dx_data_collector - Base Image Build

on:
  push:
    branches:
      - 'feature/*'
      - 'hotfix/*'
      - 'main'
      - 'develop'
    paths:
      - '.github/**'
      - 'docker/**'

jobs:
  build:
    # NOTE: This workflow previously used RIOS-internal reusable workflows.
    # Replace with your preferred container build workflow.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build base image
        run: echo "Configure base image build step for your environment. See docker/Earthfile."
