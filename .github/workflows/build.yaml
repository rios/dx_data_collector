name: dx_data_collector - Build

on:
  push:
    branches:
      - 'feature/*'
      - 'hotfix/*'
      - 'main'
      - 'develop'

jobs:
  build:
    # NOTE: This workflow previously used RIOS-internal reusable workflows.
    # To use in your own environment, replace with a standard earthly build step.
    # See https://earthly.dev for Earthfile-based build documentation.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo "Configure build step for your environment. See docker/Earthfile."
