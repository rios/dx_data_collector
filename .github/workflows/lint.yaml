name: Lint checks

on:
  pull_request:
    branches:
      - '*'

jobs:
  lint_checks:
    # NOTE: This workflow previously used RIOS-internal reusable workflows.
    # Replace with your preferred linting setup.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint
        run: echo "Configure lint step for your environment."
