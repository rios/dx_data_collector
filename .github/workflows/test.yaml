name: dx_data_collector - Test

on:
  push:
    branches:
      - 'feature/*'
      - 'hotfix/*'
      - 'main'
      - 'develop'
jobs:
  test:
    # NOTE: This workflow previously used RIOS-internal reusable workflows.
    # To use in your own environment, replace with a standard earthly test step.
    # See https://earthly.dev for Earthfile-based build documentation.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: echo "Configure test step for your environment. See docker/Earthfile."
